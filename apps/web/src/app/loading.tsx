import { LoadingSequence } from "@/components/ui/LoadingSequence";

/**
 * Root loading state — cinematic loading sequence shown during page transitions.
 */
export default function Loading() {
  return <LoadingSequence />;
}
